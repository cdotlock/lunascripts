{
  "$schema": "./contract-manifest.schema.json",
  "contract_version": "3.0.0",
  "change_class": "major",
  "episode_schema": "episode.schema.json",
  "source_spec": "../LS-SPEC.md",
  "json_output_spec": "../docs/JSON-OUTPUT.md",
  "author_signal_name_pattern": "^[A-Z][A-Z0-9_]*$",
  "author_signal_name_style": "SCREAMING_SNAKE_CASE",
  "mark_reader_name_pattern": "^[A-Z][A-Z0-9_]*$",
  "engine_value_namespace": "runtime-declared and read-only; lowercase names remain distinct from author signals",
  "valid_fixtures": "fixtures/valid",
  "invalid_fixtures": "fixtures/invalid",
  "compatibility": {
    "breaking": true,
    "legacy_unversioned_episode_json": "consumer migration concern; compiler output is always versioned",
    "migration": "Adopt the grammar-only spec: @text participants are character ids; multiplayer cross-role guidance is removed; brave checks require a non-empty attr and positive integer dc; achievement ids must match ^[A-Z][A-Z0-9_]*$; @choice directly contains at least two @option entries. Author new backgrounds as @bg <name> [transition]; the compiler continues to accept legacy @bg set <name> [transition]."
  },
  "rollout": {
    "consumer_repositories": [
      "cdotlock/lunaverse-backend",
      "cdotlock/lunaverse-ide"
    ],
    "stored_content_policy": "audit_only"
  }
}
